[project]
name = "keycloak-provisioner"
version = "0.0.0"
description = "Kubernetes operator to provision Keycloak OAuth clients via CRDs"
requires-python = ">=3.14"
dependencies = [
    "kopf>=1.37,<2",
    "httpx>=0.27,<1",
    "kubernetes>=31,<33",
]
